# Sepolia address of your deployed AsyncRevealVault.
# Set this in your hosting provider's environment (Railway, Cloudflare Workers,
# fly.io, ...). The frontend reads it at build time AND at request time.
NEXT_PUBLIC_VAULT_ADDRESS=0x0000000000000000000000000000000000000000
