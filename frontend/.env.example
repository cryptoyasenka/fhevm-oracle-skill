# Sepolia address of your deployed AsyncRevealVault.
# Set this in your hosting provider's environment (Vercel — current host —
# or Railway, Cloudflare Workers Containers, fly.io, ...). The frontend reads
# it at build time AND at request time.
NEXT_PUBLIC_VAULT_ADDRESS=0x0000000000000000000000000000000000000000
